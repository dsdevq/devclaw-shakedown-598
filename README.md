# devclaw-shakedown-598

> **Draft** — generated by devclaw onboarding for human review.

A scratch repository used to verify devclaw's onboard-delivery fix for issue #598. It contains no application source code; its purpose is to provide a clean target for exercising the onboarding pipeline end-to-end.

The only substantive content is the **speckit** Spec-Driven Development (SDD) substrate, installed automatically by devclaw under `.specify/`.

## Quickstart

```bash
git clone <this-repo>
cd devclaw-shakedown-598

# Verify the speckit substrate is healthy
bash .specify/scripts/bash/check-prerequisites.sh

# Scaffold a new feature
bash .specify/scripts/bash/create-new-feature.sh "My feature description"
```

## What's inside

```
.specify/          speckit SDD substrate (scripts, templates, workflows)
.mcp.json          Playwright MCP server config (for agent browser use)
```

See [AGENTS.md](AGENTS.md) for the detailed layout and exact commands.

## Status

Experimental / scratch — not intended for production use.
