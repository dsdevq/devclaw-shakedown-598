# DRAFT — generated by devclaw onboarding for human review.
# Dev image for devclaw-shakedown-598. The project is pure bash; Node is
# included because .mcp.json references npx for the Playwright MCP server.

FROM node:20-bookworm-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    bash \
    curl \
    jq \
 && rm -rf /var/lib/apt/lists/*
